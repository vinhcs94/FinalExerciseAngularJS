app.controller("DetailController",function($scope,storeService,$routeParams){
	
	$scope.id = $routeParams.id;
    storeService.getProduct($scope.id).then(function(data){
  	  $scope.product = data;
    });
    
    
});
