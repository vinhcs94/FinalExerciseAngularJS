app.controller("DetailController",function($scope,mainStoreService,$routeParams){
	
	$scope.id = $routeParams.id;
	mainStoreService.getProduct(id).then(function(data){
  	  $scope.product = data;
    });
    
    
});
